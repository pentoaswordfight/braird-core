# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# UniFFI / bindings build output
/bindings/**/build
/bindings/**/.gradle
/bindings/**/.build
# uniffi-bindgen output (regenerated by the Gradle build into the main source root)
/bindings/kotlin/src/main/kotlin/uniffi/
*.xcframework
/out

# Secrets — never commit
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.swp
